---
description: Specify a capability with swarm verification — reverse-engineer the source-of-truth spec (requirements + Given/When/Then scenarios) from the real code, then have independent agents spec it blind and drift-check every requirement against actual behavior.
---

Invoke the `swarm:spec` skill. Arguments: $ARGUMENTS
