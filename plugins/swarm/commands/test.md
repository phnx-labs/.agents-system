---
description: Test critical paths with a swarm — split the scope into areas, cover each in parallel, then synthesize the cross-cutting tests that span areas.
---

Invoke the `swarm:test` skill. Arguments: $ARGUMENTS
