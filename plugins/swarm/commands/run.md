---
description: Run any task across a swarm — the generic fan-out. Decompose an arbitrary goal into independent tracks, spawn a mixed team of parallel agents, monitor, and synthesize. Use when the task doesn't fit plan/debug/test/qa.
---

Invoke the `swarm:run` skill. Arguments: $ARGUMENTS
