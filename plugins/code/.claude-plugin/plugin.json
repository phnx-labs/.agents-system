{
  "name": "code",
  "version": "0.7.0",
  "description": "code — coding-workflow plugin. Sub-skills for the manager/router engineering loop: `code:loop` drains a queue end-to-end (single ticket or many) with mindset-grounded autonomy, `code:dispatch` triages a single task and picks the delivery path, `code:review` runs a sub-agent pre-merge review with file:line grounding and a security pass on risk-touching diffs, `code:verify` runs the end-to-end gate, `code:ship` is the post-merge gate for distributables (publish, confirm live, activate, verify), `code:sprint` runs a time-boxed multi-track parallel push, `code:quality` runs a read-only health diagnostic and opens an HTML report, `code:learn` reflects on a session and folds durable lessons back into the plugin. `/commit` splits the working tree into the maximum number of small logical commits.",
  "author": {
    "name": "Muqsit Nawaz",
    "email": "contact@trp.so"
  }
}
