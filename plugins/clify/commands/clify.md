---
description: Turn a web service into an installable, verified CLI — climbs docs → authed API → HAR capture → injection, infers a typed command schema, emits a CLI (and optionally an MCP server via --mcp), and ships no command until it has hit the real endpoint and returned real output.
argument-hint: "[target — e.g. 'stripe.com', 'the API behind app.linear.app', 'https://api.foo.com/docs'] [--mcp] [--lang=ts|py]"
---

Invoke the `clify:build` skill. Target and flags: $ARGUMENTS
