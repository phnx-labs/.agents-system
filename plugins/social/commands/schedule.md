---
description: Generate a coverage-deduped, engagement-rubric content backlog and schedule it to X/LinkedIn via getlate (user runs the live post step).
---

Invoke the `social:schedule` skill for: $ARGUMENTS

Read `skills/schedule/SKILL.md`: build/calibrate the coverage index, draft posts in the channel's house voice (engagement-first, no pitch, comment-prompt, AI-tells scrubbed), dedup every candidate, lay out the Tue/Wed/Thu schedule. Publishing is gated — confirm copy + dates with the user and have THEM run schedule_posts.py.
