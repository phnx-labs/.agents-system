---
description: Second pass — score how well the content taxonomy aligns with the audience/ICP you want to reach (buyer vs amplifier), with gaps and a re-weighting.
---

Invoke the `social:align` skill for: $ARGUMENTS

Requires a completed `social:audit` run ($CA_DIR with report/taxonomy_structure.json + nodes.json). Read `skills/align/SKILL.md`: extract the real ICP from the growth agents + the amplifier targets, score all subtopics, and lead the report with the volume-weighted buyer-vs-amplifier punchline.
