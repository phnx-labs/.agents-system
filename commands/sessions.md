---
description: "List and load named sessions across agent versions"
---

Use the `sessions` skill to handle this request.

Arguments: $ARGUMENTS

If no arguments or "list": list all named sessions across versions.
If a search query (e.g., "channels", "keychain"): load that session — read transcript, scan current codebase state, summarize what's done/remaining, then start working. Don't ask permission to proceed.
