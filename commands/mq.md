---
description: Query large documents without reading everything
---

You are analyzing: $ARGUMENTS

Load the `/mq` skill and follow its workflow to query this document efficiently.

## Quick Start

```bash
mq .tree file.md       # See structure
mq .search file.md "term"  # Find relevant sections
mq .section file.md "Section Name"  # Extract specific section
```

## Rules

1. **Probe first** — See structure before reading content
2. **Search surgically** — Find relevant parts without reading everything
3. **Extract efficiently** — Pull only what you need
4. **Read only when necessary** — Full reads are a last resort

Load the skill now and proceed with the analysis.
